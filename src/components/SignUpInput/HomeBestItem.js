import ItemItem from "../ItemItem";
import ItemList from "../ItemList";

const HomeBestItem = ({ dummyList }) => {
  return (
    <div className="HomeBestItem">
      <h2>홈의 베스트 아이템을 넣어보자</h2>
    </div>
  );
};

export default HomeBestItem;
